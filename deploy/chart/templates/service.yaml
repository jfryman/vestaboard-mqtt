apiVersion: v1
kind: Service
metadata:
  name: {{ include "vestaboard-mqtt.fullname" . }}
  labels:
    {{- include "vestaboard-mqtt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vestaboard-mqtt.selectorLabels" . | nindent 4 }}
